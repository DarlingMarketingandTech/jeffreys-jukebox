# Cloudinary audio assets

The five recovered recordings are stored as public MP3 assets in Cloudinary cloud `dr0xs4iar` under `jeffreys-jukebox/audio`.

The application references versioned HTTPS delivery URLs in `lib/tracks.ts`.
